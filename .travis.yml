dist: xenial   # required for Python >= 3.7
language: python
python:
  - "3.7"
install:
  - pip install pipenv
  - pipenv install --ignore-pipfile --dev
before_script:
  - flake8 pypefitter
script:
  - pytest --cov=pypefitter